/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { VAULT_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const CLAIM_DEPOSIT_DISCRIMINATOR = new Uint8Array([
  201, 106, 1, 224, 122, 144, 210, 155,
]);

export function getClaimDepositDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLAIM_DEPOSIT_DISCRIMINATOR,
  );
}

export type ClaimDepositInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountClaimer extends string | AccountMeta<string> = string,
  TAccountDeposit extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountClaimer extends string
        ? WritableSignerAccount<TAccountClaimer> &
            AccountSignerMeta<TAccountClaimer>
        : TAccountClaimer,
      TAccountDeposit extends string
        ? WritableAccount<TAccountDeposit>
        : TAccountDeposit,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ClaimDepositInstructionData = {
  discriminator: ReadonlyUint8Array;
  depositId: bigint;
  secret: string;
};

export type ClaimDepositInstructionDataArgs = {
  depositId: number | bigint;
  secret: string;
};

export function getClaimDepositInstructionDataEncoder(): Encoder<ClaimDepositInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["depositId", getU64Encoder()],
      ["secret", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: CLAIM_DEPOSIT_DISCRIMINATOR }),
  );
}

export function getClaimDepositInstructionDataDecoder(): Decoder<ClaimDepositInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["depositId", getU64Decoder()],
    ["secret", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getClaimDepositInstructionDataCodec(): Codec<
  ClaimDepositInstructionDataArgs,
  ClaimDepositInstructionData
> {
  return combineCodec(
    getClaimDepositInstructionDataEncoder(),
    getClaimDepositInstructionDataDecoder(),
  );
}

export type ClaimDepositInput<
  TAccountClaimer extends string = string,
  TAccountDeposit extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** The user claiming the deposit (must sign) */
  claimer: TransactionSigner<TAccountClaimer>;
  /** The deposit account being claimed */
  deposit: Address<TAccountDeposit>;
  /** System program for SOL transfers */
  systemProgram?: Address<TAccountSystemProgram>;
  depositId: ClaimDepositInstructionDataArgs["depositId"];
  secret: ClaimDepositInstructionDataArgs["secret"];
};

export function getClaimDepositInstruction<
  TAccountClaimer extends string,
  TAccountDeposit extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: ClaimDepositInput<
    TAccountClaimer,
    TAccountDeposit,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ClaimDepositInstruction<
  TProgramAddress,
  TAccountClaimer,
  TAccountDeposit,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    claimer: { value: input.claimer ?? null, isWritable: true },
    deposit: { value: input.deposit ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.claimer),
      getAccountMeta(accounts.deposit),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getClaimDepositInstructionDataEncoder().encode(
      args as ClaimDepositInstructionDataArgs,
    ),
    programAddress,
  } as ClaimDepositInstruction<
    TProgramAddress,
    TAccountClaimer,
    TAccountDeposit,
    TAccountSystemProgram
  >);
}

export type ParsedClaimDepositInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The user claiming the deposit (must sign) */
    claimer: TAccountMetas[0];
    /** The deposit account being claimed */
    deposit: TAccountMetas[1];
    /** System program for SOL transfers */
    systemProgram: TAccountMetas[2];
  };
  data: ClaimDepositInstructionData;
};

export function parseClaimDepositInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedClaimDepositInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      claimer: getNextAccount(),
      deposit: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getClaimDepositInstructionDataDecoder().decode(instruction.data),
  };
}
